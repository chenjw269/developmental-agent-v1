# Database (default: SQLite in project directory)
# DATABASE_URL=sqlite:///./admin.db

# APISIX Admin API (required for approve / quota / disable / enable)
APISIX_ADMIN_URL=http://127.0.0.1:9180
APISIX_ADMIN_KEY=edd1c9f034335f136f87ad84b625c8f1

# Optional: require X-Admin-Key header for admin-only endpoints (GET /applications, approve, reject, credentials)
# If not set, admin endpoints are open (demo only).
# ADMIN_API_KEY=your-admin-secret
