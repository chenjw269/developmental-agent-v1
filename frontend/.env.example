# 构建时生效，用于生产/ Docker 部署
# 开发时可在 vite.config.ts 中配置 proxy，或在此填写实际地址

# 管理后端 API 根地址
# VITE_ADMIN_API_BASE=http://localhost:8000

# APISIX 网关地址（用户带 API Key 访问智能体）
# VITE_GATEWAY_BASE=http://localhost:9080
